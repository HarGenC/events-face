# Events Face

Backend-сервис для отображения мероприятий и регистрации пользователей на них.

Данный проект демонстрирует работу с Django, Django REST Framework, JWT-аутентификацией и паттерном Outbox для надёжной отправки уведомлений.

---

## Возможности

* Получение списка мероприятий
* Регистрация пользователя на мероприятие
* Защита эндпоинтов с помощью JWT
* Отправка уведомлений через Outbox worker
* Работа с переменными окружения через `.env`

---

## 🛠 Технологии

* Python 3.12+
* Django
* Django REST Framework
* SimpleJWT
* SQLite

---

## ⚙️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repo_url>
cd Django
```

### 2. Виртуальное окружение

```bash
python -m venv .venv
source .venv/bin/activate  # Linux / MacOS
.venv\\Scripts\\activate     # Windows
```

### 3. Установка зависимостей

```bash
pip install -r uv.lock
```

---

## Переменные окружения

Создай свой файл `.env` в корне проекта:

Пример переменных файла доступен в `.env.example`.

---

## 🗄 Миграции и суперпользователь

```bash
python manage.py migrate
python manage.py createsuperuser
```

---

## Запуск сервера

```bash
python manage.py runserver
```

API будет доступно по адресу:

```
http://localhost:8000/api/
```

---

## Основные эндпоинты

| Метод | URL                        | Описание                   |
| ----- | -------------------------- | -------------------------- |
| GET   | /api/events/               | Список мероприятий         |
| POST  | /api/events/{id}/register/ | Регистрация на мероприятие |
| POST  | /api/auth/register         | Регистрация пользователя   |
| POST  | /api/auth/login            | Аутентификация пользователя|
| POST  | /api/auth/logout           | Выход из системы           |
| POST  | /api/auth/token/           | Получение JWT              |
| POST  | /api/auth/token/refresh/   | Обновление JWT             |

---

## Outbox worker

Для отправки уведомлений используется паттерн Outbox.

Запуск воркера:

```bash
python manage.py worker
```

Воркер:

* Получает неотправленные сообщения из OutboxMessage
* отправляет их во внешний сервис уведомлений
* помечает как `sent=True` только при статусах (201, 409)

---

## Sync

В проекте реализован механизм синхронизации мероприятий между внешним сервисом источником данных и локальной базой данных сервиса events-face.

Инкрементальная синхронизация (по умолчанию):

```bash
python manage.py sync_events
```

Поведение:

* если дата не указана, используется дата последней успешной синхронизации
* загружаются только мероприятия, изменённые после этой даты

Полная синхронизация всех мероприятий:

```bash
python manage.py sync_events --all
```

Поведение:

* загружаются все мероприятия из внешнего сервиса
* используется для первой синхронизации или полного обновления данных

```bash
python manage.py sync_events --date 2025-03-15
```

Поведение:

* отдает все мероприятия которые измнены в указанную дату или позже

---

## 👤 Автор

Valentin Politov
Python Backend Developer